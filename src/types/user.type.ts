export type AuthUserType = {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};

export type UserType = {
  email: string;
  firstName: string;
  lastName: string;
  isAuth: boolean;
};
